export default function WardrobePlaceholder() {
  return (
    <div>
      <h1 className="display">Wardrobe</h1>
      <p className="lede">
        Your gallery lives here. We’re building it next — for now, head to{' '}
        <a href="/upload" style={{ color: 'var(--gold)' }}>Add items</a> to load your closet.
      </p>
    </div>
  );
}
